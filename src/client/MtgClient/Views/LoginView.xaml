<UserControl x:Class="MtgClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MtgClient.ViewModels">
    <UserControl.DataContext>
        <vm:LoginViewModel />
    </UserControl.DataContext>

    <Grid Background="#1a1a2e">
        <Border Background="#16213e" CornerRadius="12" Padding="40"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                MinWidth="400" MaxWidth="500">
            <StackPanel>
                <!-- Title -->
                <TextBlock Text="⚔ MTG Commander Engine"
                           FontSize="28" FontWeight="Bold"
                           Foreground="#e94560" HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Connectez-vous au serveur"
                           FontSize="14" Foreground="#8899aa"
                           HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <!-- Server address -->
                <TextBlock Text="Adresse du serveur" Foreground="#cccccc"
                           FontSize="12" Margin="0,0,0,4"/>
                <TextBox Text="{Binding ServerAddress, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputTextBox}"
                         Margin="0,0,0,16"/>

                <!-- Player name -->
                <TextBlock Text="Nom du joueur" Foreground="#cccccc"
                           FontSize="12" Margin="0,0,0,4"/>
                <TextBox Text="{Binding PlayerName, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputTextBox}"
                         Margin="0,0,0,24"/>

                <!-- Error message -->
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#e94560" FontSize="12"
                           TextWrapping="Wrap"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                           Margin="0,0,0,16"/>

                <!-- Connect button -->
                <Button Content="{Binding IsLoading, Converter={StaticResource BoolToConnectTextConverter}}"
                        Command="{Binding ConnectCommand}"
                        Style="{StaticResource PrimaryButton}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                        Height="44"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
